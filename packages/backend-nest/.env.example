# ==============================================================================
# Redis 環境變數配置
# ==============================================================================
# 複製此檔案為 .env 並根據環境調整

# Redis 主機位址
# 本地開發：localhost 或 127.0.0.1
# Docker Compose：redis（服務名稱）
# 遠端伺服器：實際 IP 或域名
REDIS_HOST=localhost

# Redis 埠號
# 預設：6379
REDIS_PORT=6379

# Redis 資料庫編號（0-15，預設 0）
REDIS_DB=0

# Redis 密碼（若無則留空或移除）
# 本地開發通常不需要密碼
# 生產環境務必設定強密碼
# REDIS_PASSWORD=your_secure_password

# Redis TTL（Time To Live）- 鍵過期時間（秒）
# 預設：86400（24 小時）
# 可根據遊戲進度的生命週期調整
# REDIS_TTL=86400

# ==============================================================================
# 應用程式配置
# ==============================================================================

# 應用埠號
PORT=3000

# 環境模式（development / production）
NODE_ENV=development

# 資料儲存類型（redis / memory）
STORAGE_TYPE='memory'

# ==============================================================================
# 使用說明
# ==============================================================================
# 1. 複製此檔案：cp .env.example .env
# 2. 根據環境編輯 .env
# 3. 不要將 .env 提交至 Git（已在 .gitignore）
# 4. 啟動 Redis：docker-compose up -d
# 5. 啟動應用：npm run start:dev
