# 潛在錯誤風險

## 1. 模板驗證的缺失 🔴

### 問題

affix.blueprint.md 的詞綴生成**檢查 Family/Exclusion 規則**

但若模板預綁定已矛盾，實例化時會直接出錯

### 缺陷

沒有預防模板驗證步驟

```
// 例
AffixTemplate {
  id: 'affix_1',
  family: 'fireElement',
  exclusions: ['affix_1'], // 🚨 自己排斥自己
}

// 當實例化時
try {
  const instance = createAffixInstance(template);
  // ❌ 錯誤：排斥規則檢查失敗
} catch (e) {
  // 已經太晚了，玩家看到了
}
```

### 風險

- **線上 bug**：玩家遊戲崩潰
- **資料污染**：壞模板傳播
- **難以追蹤**：模板定義可能在多個地方

### 建議

實現模板驗證層

```typescript
// Step 1：定義驗證規則
interface AffixTemplateValidator {
  validate(template: AffixTemplate): ValidationResult
}

class AffixTemplateValidatorImpl implements AffixTemplateValidator {
  validate(template: AffixTemplate): ValidationResult {
    const errors: string[] = []

    // 檢查自排斥
    if (template.exclusions.includes(template.id)) {
      errors.push(`Affix ${template.id} 自排斥自己`)
    }

    // 檢查家族衝突
    if (template.family && template.exclusions) {
      for (const excluded of template.exclusions) {
        const excludedTemplate = this.pool.get(excluded)
        if (excludedTemplate?.family === template.family) {
          errors.push(`Affix ${template.id} 和 ${excluded} 同家族卻排斥`)
        }
      }
    }

    return {
      isValid: errors.length === 0,
      errors,
    }
  }
}

// Step 2：在啟動時驗證所有模板
function initializeAffixPool(templates: AffixTemplate[]) {
  const validator = new AffixTemplateValidatorImpl()

  for (const template of templates) {
    const result = validator.validate(template)
    if (!result.isValid) {
      // 啟動時失敗，便於發現
      throw new Error(`Invalid Affix Template: ${template.id}\n${result.errors.join('\n')}`)
    }
  }

  // 驗證通過，才初始化
  affixPool = new Map(templates.map((t) => [t.id, t]))
}
```

---

## 2. 同 Tick 內事件順序的隨機性風險 🔴

### 問題

combat.blueprint.md 同 Tick 內事件順序需一致

但若致死攻擊與恢復事件同時發生，依賴隊列順序可能導致隨機結果

### 缺陷

違反「非隨機原則」

```
// 場景
Tick 內：
- Event A: 玩家攻擊敵人，造成 100 傷害，敵人 HP 變成 0（致死）
- Event B: 同時，敵人復活技能（被動），回復 50 HP

問題：
- 誰先執行？
- 隊列順序不明確？

結果 1：A → B（敵人活著）
結果 2：B → A（敵人死亡）

同一 Save 檔，重播結果不同 👈 BUG
```

### 風險

- **重播不確定性**：同樣的輸入產生不同結果
- **前後端不同步**：一個 Tick 可能有多種結果
- **玩家投訴**：「為什麼重放和真實戰鬥不一樣」

### 建議

**明確定義 Tick 內事件優先級**

```typescript
// 定義優先級
enum EventPriority {
  CRITICAL = 0, // 致死判定、狀態移除
  DAMAGE = 1, // 傷害計算
  RECOVERY = 2, // 恢復
  STATUS = 3, // 狀態應用
  BUFF = 4, // 增益
}

// 致死事件必須排隊在復活之前
function sortTickEvents(events: TickEvent[]): TickEvent[] {
  return events.sort((a, b) => {
    // 首先按優先級
    if (a.priority !== b.priority) {
      return a.priority - b.priority
    }

    // 優先級相同時，按時間戳（確定性）
    return a.timestamp - b.timestamp
  })
}

// 或者明確禁止同 Tick 內致死 + 復活
function validateTickEvents(events: TickEvent[]): boolean {
  const hasDeathEvent = events.some((e) => e.type === 'DEATH')
  const hasResurrectionEvent = events.some((e) => e.type === 'RESURRECTION')

  if (hasDeathEvent && hasResurrectionEvent) {
    throw new Error('同 Tick 內不能同時有致死和復活事件，設計有誤')
  }

  return true
}
```

---

## 3. 多設備登入的代幣不同步 ⚠️

### 問題

endless.blueprint.md 移除剩餘重生代幣

但若玩家多設備登入，代幣狀態不同步可能導致重生邏輯失效

### 缺陷

```
// 場景
時刻 1：玩家在設備 A 用掉 1 個重生代幣
時刻 2：未同步到服務器（網路延遲）
時刻 3：玩家在設備 B 查詢代幣數量（舊快取？）

結果：
- 設備 A：0 代幣
- 設備 B：1 代幣
- 誰是真相？

玩家重生時：
- 設備 A：成功（已確認使用）
- 設備 B：失敗或重複計算
```

### 風險

- **規則被繞過**：無限重生
- **資料不一致**：玩家看到不同的狀態
- **運營損失**：無法控制重生次數

### 建議

實現多設備同步與版本控制

```typescript
// 結構
interface UserState {
  version: number // 版本號，每次改變遞增
  resurrectionTokens: number
  lastModified: number // 時戳
}

// 消耗代幣
async function useResurrectionToken(userId: string) {
  const user = await db.getUser(userId)

  if (user.resurrectionTokens <= 0) {
    throw new Error('沒有重生代幣')
  }

  // 樂觀鎖
  const success = await db.updateUser(
    {
      userId,
      version: user.version, // 檢查版本
    },
    {
      resurrectionTokens: user.resurrectionTokens - 1,
      version: user.version + 1, // 版本遞增
      lastModified: Date.now(),
    }
  )

  if (!success) {
    // 版本不匹配，說明其他設備已改
    throw new Error('代幣狀態已更新，請重新同步')
  }

  return { success: true }
}

// 多設備同步
async function syncUserState(userId: string) {
  const user = await db.getUser(userId)

  return {
    version: user.version,
    resurrectionTokens: user.resurrectionTokens,
    // ... 其他狀態
  }
}
```

---

## 4. 物品分類擴展的兼容性問題 ⚠️

### 問題

inventory.blueprint.md 整理功能**僅限裝備與遺物**

但若其他物品類型增加，缺少擴展機制會導致分類錯誤

### 缺陷

```typescript
// 當前實現
function organizeInventory(inventory: Item[]) {
  const equipment = inventory.filter((i) => i.type === 'equipment')
  const artifacts = inventory.filter((i) => i.type === 'artifact')

  return { equipment, artifacts }
  // 其他類型？都被忽略了
}

// 新增類型時
// 需要改代碼 ❌
function organizeInventory(inventory: Item[]) {
  const equipment = inventory.filter((i) => i.type === 'equipment')
  const artifacts = inventory.filter((i) => i.type === 'artifact')
  const newType = inventory.filter((i) => i.type === 'newType')
  // ... 手動添加，容易遺漏
}
```

### 風險

- **遺漏整理**：新物品類型無法整理
- **難以擴展**：每次新增類型都要改代碼
- **測試遺漏**：新類型的測試容易忘記

### 建議

使用配置驅動的分類

```typescript
// 定義物品類型與分類規則
interface ItemCategoryConfig {
  type: ItemType
  category: 'equipment' | 'artifact' | 'consumable' | 'quest'
  sortOrder: number
}

const ITEM_CATEGORIES: ItemCategoryConfig[] = [
  { type: 'weapon', category: 'equipment', sortOrder: 1 },
  { type: 'armor', category: 'equipment', sortOrder: 2 },
  { type: 'artifact', category: 'artifact', sortOrder: 1 },
  { type: 'potion', category: 'consumable', sortOrder: 1 },
]

// 通用整理函數
function organizeInventory(inventory: Item[], config: ItemCategoryConfig[] = ITEM_CATEGORIES) {
  const result = new Map<string, Item[]>()

  // 按配置分組
  for (const item of inventory) {
    const categoryConfig = config.find((c) => c.type === item.type)
    const category = categoryConfig?.category ?? 'other'

    if (!result.has(category)) {
      result.set(category, [])
    }

    result.get(category)!.push(item)
  }

  // 按配置排序
  for (const [category, items] of result) {
    items.sort((a, b) => {
      const configA = config.find((c) => c.type === a.type)?.sortOrder ?? 999
      const configB = config.find((c) => c.type === b.type)?.sortOrder ?? 999
      return configA - configB
    })
  }

  return result
}

// 新增類型時，只需在配置中添加
ITEM_CATEGORIES.push({ type: 'skill', category: 'skill', sortOrder: 1 })
// 已自動支援
```

---

## 5. 多設備同步的 UI 不同步錯誤 ⚠️

### 問題

shop.blueprint.md 多設備同步**拒絕舊請求**

但若網路延遲，最新狀態回傳可能與玩家預期不符

### 缺陷

```
// 場景
時刻 1：玩家在設備 A 購買物品 X（成功）
時刻 2：網路延遲，確認消息遲到
時刻 3：玩家在設備 B 看到依然有物品 X（快取舊狀態）
時刻 4：嘗試在設備 B 再購買 X（失敗，因為已售出）

結果：
UI 顯示有 X → 購買失敗 → 玩家困惑
```

### 風險

- **玩家困惑**：UI 與後端狀態不一致
- **操作失敗**：「明明有，為什麼不能買」
- **投訴**：以為是 bug

### 建議

實現樂觀更新 + 回滾機制

```typescript
// 前端：樂觀更新
async function purchaseItemOptimistic(itemId: string) {
  // Step 1：立即更新 UI（樂觀）
  removeItemFromShop(itemId)
  addItemToInventory(itemId)

  try {
    // Step 2：發送請求
    const result = await api.purchase(itemId)

    if (result.success) {
      // Step 3：確認（不需要更新 UI，已經改了）
      showNotification('購買成功')
    } else {
      // Step 4：失敗，回滾
      throw new Error(result.error)
    }
  } catch (error) {
    // Step 5：出錯，回滾 UI
    addItemToShop(itemId)
    removeItemFromInventory(itemId)
    showNotification(`購買失敗：${error.message}`)
  }
}

// 後端：防止重複購買
class ShopService {
  async purchase(userId: string, itemId: string) {
    const shop = await this.getShop(userId)

    // 檢查物品是否存在
    if (!shop.items.find((i) => i.id === itemId)) {
      throw new Error('物品已售出或不存在')
    }

    // 檢查玩家是否有足夠金幣
    const user = await db.getUser(userId)
    const item = shop.items.find((i) => i.id === itemId)

    if (user.gold < item.price) {
      throw new Error('金幣不足')
    }

    // 執行購買（原子操作）
    const success = await db.transaction(() => {
      db.removeFromShop(userId, itemId)
      db.addToInventory(userId, itemId)
      db.updateGold(userId, user.gold - item.price)
    })

    return { success }
  }
}
```

---

## 6. 屬性聚合的多來源計算不一致 ⚠️

### 問題

stat.blueprint.md 聚合系統**僅處理 StatModifier**

但若來源解析規則未統一，多來源可能導致計算不一致或遺漏

### 缺陷

```typescript
// 分散的解析邏輯
class StatAggregator {
  aggregate(unit: Unit): Stats {
    let modifiers: Modifier[] = []

    // 來源 1：Affix
    for (const affix of unit.affixes) {
      modifiers.push(...parseAffix(affix)) // 自定義邏輯 A
    }

    // 來源 2：Status
    for (const status of unit.statuses) {
      modifiers.push(...parseStatus(status)) // 自定義邏輯 B
    }

    // 來源 3：技能（另一個地方實現）
    modifiers.push(...getSkillModifiers(unit.skill))

    // 來源 4：賽前變數（又是別的地方）
    modifiers.push(...getPreCombatModifiers(unit))

    // 問題：
    // - 四種不同的解析邏輯
    // - 若某個邏輯有 bug，難以追蹤
    // - 新增來源時需要改這裡
    // - 優先級混亂

    return this.finalAggregate(modifiers)
  }
}

// 結果：容易漏掉某個來源或不同優先級
```

### 風險

- **計算錯誤**：某個來源的 Modifier 沒算
- **不確定性**：同樣的單位在不同時刻屬性不同
- **難以測試**：需要模擬所有來源

### 建議

統一的解析管道

```typescript
// 定義所有 Modifier 來源
interface ModifierSource {
  affixes: AffixInstance[]
  statuses: StatusInstance[]
  skillModifiers: SkillModifier[]
  preCombatModifiers: PreCombatModifier[]
  // 新增來源只需加一行
}

// 統一解析
interface IModifierResolver {
  resolve(source: ModifierSource): Modifier[]
}

class UnifiedModifierResolver implements IModifierResolver {
  constructor(
    private affixResolver: IAffixResolver,
    private statusResolver: IStatusResolver,
    private skillResolver: ISkillResolver,
    private preCombatResolver: IPreCombatResolver
  ) {}

  resolve(source: ModifierSource): Modifier[] {
    return [
      ...this.affixResolver.resolve(source.affixes),
      ...this.statusResolver.resolve(source.statuses),
      ...this.skillResolver.resolve(source.skillModifiers),
      ...this.preCombatResolver.resolve(source.preCombatModifiers),
    ]
  }
}

// 使用
class StatAggregator {
  constructor(private resolver: IModifierResolver) {}

  aggregate(unit: Unit): Stats {
    const source: ModifierSource = {
      affixes: unit.affixes,
      statuses: unit.statuses,
      skillModifiers: unit.skillModifiers,
      preCombatModifiers: unit.preCombatModifiers,
    }

    const modifiers = this.resolver.resolve(source)
    return this.finalAggregate(modifiers)
  }
}
```

---

## 重點梳理

| 錯誤         | 類型   | 優先級 | 影響       |
| ------------ | ------ | ------ | ---------- |
| 模板驗證缺失 | 設計   | 🔴 高  | 啟動崩潰   |
| 事件順序隨機 | 邏輯   | 🔴 高  | 重播不同步 |
| 代幣不同步   | 規則   | 🟡 中  | 玩家利用   |
| 物品分類遺漏 | 擴展   | 🟡 中  | 無法整理   |
| UI 不同步    | 一致性 | 🟡 中  | 玩家困惑   |
| 聚合不一致   | 計算   | 🔴 高  | 屬性錯誤   |

---

## 標記

- 總體：高優先級問題 3 個，需立即解決
- 解決順序：模板驗證 > 事件順序 > 聚合一致性
