# 本地開發：localhost 或 127.0.0.1
# Docker Compose：redis（服務名稱）
# 遠端伺服器：實際 IP 或域名
REDIS_HOST=localhost

# Redis 埠號
# 預設：6379
REDIS_PORT=6379

# Redis 資料庫編號（0-15，預設 0）
REDIS_DB=0

PORT=3000
# 環境模式（development / production）
NODE_ENV=development

# 資料儲存類型（redis / memory）
STORAGE_TYPE='redis'
JWT_SECRET='GenerateASecretKeyForJWT Here'

