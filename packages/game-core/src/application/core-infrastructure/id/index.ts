/**
 * ID 生成助手导出
 *
 * 使用方式：
 * import { IdGeneratorHelper } from '@core-infrastructure/id'
 *
 * const runId = IdGeneratorHelper.generateRunId()
 * const charId = IdGeneratorHelper.generateCharacterId()
 * const relicIds = IdGeneratorHelper.generateRelicRecordIds(5)
 */
export { IdGeneratorHelper } from './idGeneratorHelpers'
